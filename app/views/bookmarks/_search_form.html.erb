<div class="row">
  <div class="col-sm-10 offset-sm-1 col-md-8 offset-md-2 col-lg-6 offset-lg-3">
    <%= form_tag request.path, method: :get, class: 'search with-turbolinks', enforce_utf8: false, id: 'search_form' do |f| %>
      <%= text_field_tag :q, params[:q], placeholder: 'Enter search term' %>
      <%= link_to '×', params.permit.merge(q: nil), style: params.has_key?(:q) ? '' : 'display:none' %>
    <% end %>
  </div>
</div>

